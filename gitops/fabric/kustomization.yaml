apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# GPU fabric applied by the Argo `gpu-fabric` Application (sync wave -1):
#   - nccl-rdma-installer.yaml: vendored NCCL/GIB DaemonSet (pinned for GitOps).
#   - gke-network-objects.yaml: rendered by Terraform (gvnic-1, rdma-0..7).
resources:
  - nccl-rdma-installer.yaml
  - gke-network-objects.yaml
