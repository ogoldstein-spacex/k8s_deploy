# Terraform
terraform/.terraform/
*.tfstate
*.tfstate.*
crash.log
terraform/terraform.tfvars

# Local secrets (SSH keys for Jupyter -> Slurm)
.secrets/

# NOTE: gitops/ (including gitops/rendered/ and gitops/bootstrap/) is rendered by
# Terraform and MUST be committed so Argo CD can read it. Do not ignore it.
